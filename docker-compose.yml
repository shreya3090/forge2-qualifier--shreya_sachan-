version: '3.8'

services:
  kanban:
    build: .
    container_name: openclaw-kanban
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped